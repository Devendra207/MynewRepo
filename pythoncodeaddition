# Input from the user
num1 = float(input("Enter the first number: "))
num2 = float(input("Enter the second number: "))

# Adding the two numbers
sum = num1 + num2

# Displaying the result
print("The sum of", num1, "and", num2, "is:", sum)


this is the code 
from chennai dev2 added code 
new code from dev1

finalized 
